(() => {
  let min = prompt(`Enter first number`);
  let max = prompt(`Enter higher number`);
  let current = prompt(`Enter last number`);

  if (min > max) {
    alert(`Try again, please. This is not right.`);
  }
})();
